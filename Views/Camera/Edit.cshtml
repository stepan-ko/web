@model CameraViewModel

<h2>Редактировать камеру</h2>

<form asp-action="Edit" method="post">

    <input type="hidden" asp-for="Id" />

    <partial name="_CameraForm" model="Model" />

    <button type="submit" class="btn btn-success">
        Сохранить
    </button>

</form>